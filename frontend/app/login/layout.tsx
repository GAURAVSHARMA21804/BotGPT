import type { ReactNode } from "react";

export const metadata = {
  title: "BotGPT — Sign In",
  description: "Sign in or create an account for BotGPT"
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return children;
}
