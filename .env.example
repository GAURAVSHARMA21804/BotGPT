APP_ENV=dev
LOG_LEVEL=INFO
GROQ_API_KEY=
DATABASE_URL=postgresql+psycopg2://gaurav:Ga2001pillu14##@localhost:5432/botgpt
REDIS_URL=redis://redis:6379/0

# JWT — set a long random string in production
JWT_SECRET=change-me-in-production-use-openssl-rand-hex-32

# Optional: comma-separated origins for browser → API (if not using Next rewrites)
CORS_ORIGINS=http://localhost:8000

# Next.js (server) proxy target when running `next dev` on host
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
