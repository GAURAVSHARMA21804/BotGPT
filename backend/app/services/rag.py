class RAGService:
    async def retrieve_context(self, query: str) -> list[str]:
        # Placeholder retrieval until vector store integration is added.
        return [f"Retrieved context for: {query}"]
